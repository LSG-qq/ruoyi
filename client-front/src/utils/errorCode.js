/**
 * 后端响应码 → 前端兜底文案。
 *
 * <p>什么情况下会用到这张表：响应拦截器取错误信息的顺序是
 * `errorCode[code] || res.data.msg || errorCode['default']`，
 * 也就是说**后端下发的 msg 优先**。只有后端没给 msg 时（例如网关或框架层直接返回 403）
 * 才回落到这里的文案。</p>
 *
 * <p>因此这里只放「后端不会给 msg」的那几个传输层状态码，
 * 业务错误（库存不足、重复申请等）一律由后端在 msg 里说清楚，
 * 前端不重写、也不在这里列 —— 两边各写一套文案必然会出现口径不一致。</p>
 *
 * <p>取值与 ruoyi-ui 的 errorCode.js 保持逐字一致：两端共用同一个后端，
 * 同一个状态码在两边显示不同的话，排查时反而要多想一层。</p>
 */
export default {
  /** 未认证 / 令牌失效。注：401 在拦截器里会走「重新登录」确认框，本文案实际只在极端兜底时显示 */
  '401': '认证失败，无法访问系统资源',

  /** 已认证但无权访问。终端接口一律不挂权限注解，理论上不会出现，留着是为了覆盖框架层拦截 */
  '403': '当前操作没有权限',

  /** 资源不存在（例如拼错了接口路径） */
  '404': '访问资源不存在',

  /** 兜底：拿不到 code 或 code 不在上表内时使用 */
  'default': '系统未知错误，请反馈给管理员'
}
